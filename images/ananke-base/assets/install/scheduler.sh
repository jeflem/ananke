#!/bin/bash

source /opt/conda/etc/profile.d/conda.sh

#conda activate jhub

#pip install --root-user-action=ignore jupyter_scheduler

echo "Due to an unfixable version missmatch jupyter_scheduler is currently not available in Ananke!"
